#include<iostream>
#include<conio.h>
#include<cstring>

using namespace std;

char* cat(const char str1[], const char str2[])
{
	int length;
	length = strlen(str1);
	length += strlen(str2);
	char arr[length + 1];
	for(int i(0); i < length )
}

int main()
{

	_getch();
	return 0;
}